<UserControl x:Class="ParcInformatiqueApp.Views.EmployeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource BackgroundColor}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">

            <TextBlock Text="Employés"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextColor}"
                       Margin="0,0,0,15"/>

            <Button Content="+ Nouvel Employé"
                    Width="160"
                    HorizontalAlignment="Left"
                    Click="BtnAddEmploye_Click"/>

        </StackPanel>

        <DataGrid x:Name="DgEmployes"
                  Grid.Row="1"
                  Margin="0,20,0,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Nom"
                                    Binding="{Binding Nom}"
                                    Width="*"/>

                <DataGridTextColumn Header="Prénom"
                                    Binding="{Binding Prenom}"
                                    Width="*"/>

                <DataGridTextColumn Header="Poste"
                                    Binding="{Binding Poste}"
                                    Width="*"/>

                <DataGridTextColumn Header="Service"
                                    Binding="{Binding Service.NomService}"
                                    Width="*"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>