<UserControl x:Class="ParcInformatiqueApp.Views.TicketsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F5F7FA">

    <Grid Margin="25">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TITRE -->
        <TextBlock Text="Gestion des tickets"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="#1F2937"
                   Margin="0,0,0,20"/>

        <!-- RECHERCHE + FILTRE -->
        <Grid Grid.Row="1"
              Margin="0,0,0,20">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtSearch"
                     Height="40"
                     FontSize="14"
                     Padding="10"
                     VerticalContentAlignment="Center"
                     TextChanged="TxtSearch_TextChanged"/>

            <ComboBox x:Name="CboStatut"
                      Grid.Column="1"
                      Height="40"
                      Margin="15,0,0,0"
                      SelectedIndex="0"
                      SelectionChanged="CboStatut_SelectionChanged">

                <ComboBoxItem Content="Tous les statuts"/>
                <ComboBoxItem Content="En attente"/>
                <ComboBoxItem Content="En cours"/>
                <ComboBoxItem Content="Terminé"/>

            </ComboBox>

        </Grid>

        <!-- TABLEAU DES TICKETS -->
        <DataGrid x:Name="DgTickets"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  RowHeight="45"
                  FontSize="14">

            <DataGrid.Columns>

                <!-- Ticket -->
                <DataGridTextColumn Header="Ticket"
                                    Binding="{Binding NomTicket}"
                                    Width="*"/>

                <!-- Employé -->
                <DataGridTemplateColumn Header="Employé"
                                        Width="180">

                    <DataGridTemplateColumn.CellTemplate>

                        <DataTemplate>

                            <TextBlock>

                                <Run Text="{Binding UserCreateur.Employe.Prenom}"/>
                                <Run Text=" "/>
                                <Run Text="{Binding UserCreateur.Employe.Nom}"/>

                            </TextBlock>

                        </DataTemplate>

                    </DataGridTemplateColumn.CellTemplate>

                </DataGridTemplateColumn>

                <!-- Date -->
                <DataGridTextColumn Header="Date"
                                    Binding="{Binding DateCreation, StringFormat=dd/MM/yyyy}"
                                    Width="110"/>

                <!-- Priorité -->
                <DataGridTextColumn Header="Priorité"
                                    Binding="{Binding Priorite}"
                                    Width="110"/>

                <!-- Statut -->
                <DataGridTextColumn Header="Statut"
                                    Binding="{Binding Statut}"
                                    Width="110"/>

                <!-- Équipement -->
                <DataGridTextColumn Header="Équipement"
                                    Binding="{Binding Equipement.NomEquipement}"
                                    Width="150"/>

                <!-- Technicien -->
                <DataGridTextColumn Header="Technicien"
                                    Binding="{Binding UserTraiteur.Employe.Nom}"
                                    Width="150"/>

                <!-- ACTIONS -->
                <DataGridTemplateColumn Header="Actions"
                                        Width="230">

                    <DataGridTemplateColumn.CellTemplate>

                        <DataTemplate>

                            <StackPanel Orientation="Horizontal">

                                <Button Content="Détails"
                                        Width="90"
                                        Height="30"
                                        Margin="0,0,5,0"
                                        Click="BtnDetails_Click"/>

                                <Button Content="Affecter"
                                        Width="90"
                                        Height="30"
                                        Click="BtnAffecter_Click"/>

                            </StackPanel>

                        </DataTemplate>

                    </DataGridTemplateColumn.CellTemplate>

                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>